apiVersion: v1
kind: ConfigMap
metadata:
  name: model-serving-config
  namespace: {{ .Release.Namespace }}
data:
  model-config.json: |
    {
      "models": {{ .Values.modelServing.models | toJson }}
    }